# 🎨 AI Office Dashboard - 設計文件

## 1. 視覺設計

### 1.1 整體風格
- **像素藝術** (Pixel Art) 為主
- 16x16 或 32x32 基礎網格
- 色調：深色背景 + 霓虹高亮（參考參考圖）
- 動畫：流暢的幀動畫，2-8 幀循環

### 1.2 色彩系統

```
背景色系：
- 主背景：#1a1a2e (深藍黑)
- 卡片背景：#16213e (深藍)
- 邊框：#0f3460 (藍灰)

強調色：
- 主色：#e94560 (珊瑚紅)
- 次色：#00d9ff (霓虹藍)
- 成功：#00ff88 (綠)
- 警告：#ffaa00 (橙)
- 數字：#00ff88 / #ff6b6b
```

### 1.3 字體
- 標題：像素字體 (Press Start 2P / VT323)
- 內文：思源黑體 / Noto Sans TC
- 數字：等寬字體

---

## 2. 頁面結構

### 2.1 主頁面佈局

```
┌────────────────────────────────────────────────────┐
│  🏢 Lucy AI 辦公室                    [設定] [?]   │
├────────────────────────────────────────────────────┤
│                                                     │
│  ┌─────────────────────────────────────────────┐  │
│  │                                              │  │
│  │         【 像素辦公室場景 】                 │  │
│  │                                              │  │
│  │    Lucy    小財    阿新    研研    螃蟹      │  │
│  │     💻      📊      📰      📚      🦀      │  │
│  │                                              │  │
│  └─────────────────────────────────────────────┘  │
│                                                     │
├────────────────────────────────────────────────────┤
│  [💰 成本節省]  [📊 互動統計]  [💬 員工心聲]      │
├────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ NT$3,581 │ │   0.1    │ │   455    │          │
│  │ 總節省    │ │ 等同員工  │ │ 本月任務  │          │
│  └──────────┘ └──────────┘ └──────────┘          │
│                                                     │
│  🏆 員工排行榜                                     │
│  ┌────────────────────────────────────────────┐  │
│  │ 員工    任務   完成   節省金額   工時       │  │
│  │ Lucy    144    8     NT$1,408   8.3h      │  │
│  │ 小財     16   15     NT$1,193    7h       │  │
│  │ ...                                        │  │
│  └────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

### 2.2 Tab 內容

#### Tab 1: 成本節省
- 總節省金額 (NT$)
- 等同全職員工數
- 本月任務總數
- 平均每日任務
- 員工排行榜（按節省金額）

#### Tab 2: 互動統計
- 今日對話數
- Token 使用量（實際）
- API 花費 (USD)
- Input/Output/Cache Read 細項
- 各員工今日互動明細

#### Tab 3: 員工心聲
- 每個員工一張卡片
- 像素頭像 + 表情 emoji
- 動態生成的吐槽文字
- 對話數、完成數、Token 等小數據

---

## 3. 角色系統

### 3.1 角色屬性

```typescript
interface Character {
  id: string;
  name: string;           // 顯示名稱
  title: string;          // 職稱
  description: string;    // 角色描述
  salary: number;         // 虛擬月薪 (NT$)
  skills: string[];       // 技能標籤
  personality: string;    // 個性設定（用於生成心聲）
  sprite: {
    idle: string[];       // 閒置動畫幀
    working: string[];    // 工作動畫幀
    happy: string[];      // 開心動畫幀
    tired: string[];      // 疲倦動畫幀
  };
  stats: {
    tasks: number;        // 任務數
    completed: number;    // 完成數
    tokens: number;       // Token 用量
    apiCost: number;      // API 花費
    workHours: number;    // 工時
  };
}
```

### 3.2 心聲生成規則

```typescript
function generateVoice(char: Character): string {
  const { stats, personality } = char;
  
  // 根據狀態選擇模板
  if (stats.completed / stats.tasks === 1) {
    return templates.perfectDay[personality];
  }
  if (stats.workHours > 8) {
    return templates.overwork[personality];
  }
  if (stats.tasks === 0) {
    return templates.idle[personality];
  }
  // ...更多邏輯
}

const templates = {
  perfectDay: {
    cheerful: "今天任務全部搞定！可以早點下班嗎 ✨",
    sarcastic: "100% 完成率，不愧是我 😏",
    anxious: "全部完成了...但會不會有什麼漏掉的 😰"
  },
  overwork: {
    cheerful: "工作 8 小時+，但看到成果很值得！💪",
    sarcastic: "社畜 AI 的日常，不說了 😮‍💨",
    anxious: "好累...明天還要繼續嗎 😴"
  }
  // ...
};
```

---

## 4. 資料架構

### 4.1 資料來源

```typescript
// 從 OpenClaw 取得
interface OpenClawData {
  sessions: Session[];      // sessions_list
  cronJobs: CronJob[];      // cron list
  cronRuns: CronRun[];      // cron runs history
}

// 衍生統計
interface DashboardStats {
  totalTasks: number;
  totalTokens: number;
  totalCost: number;
  costSaved: number;        // 假設人工成本 vs API 成本
  equivalentFTE: number;    // 等同全職員工
  characterStats: CharacterStats[];
}
```

### 4.2 任務到角色的映射

```typescript
const taskToCharacter: Record<string, string> = {
  // Cron Job ID -> Character ID
  "記憶體股盯盤提醒": "xiaocai",
  "每日美股晨報": "xiaocai",
  "AI 新聞日報": "axin",
  "GitHub 熱門專案日報": "axin",
  "Nightly Build": "yanyan",
  "夜間自主探索": "yanyan",
  "Moltbook 社群檢查": "pangxie",
  "信用卡帳單提醒": "xiaoguan",
  // sub-agent spawn -> 根據 label 或 task 內容分類
};
```

### 4.3 成本節省計算

```typescript
function calculateSavings(stats: DashboardStats): number {
  // 假設：
  // - 人工處理一個任務平均 15 分鐘
  // - 時薪 NT$250（月薪 4 萬 / 160 小時）
  const humanCostPerTask = 250 * 0.25; // NT$62.5
  const humanCost = stats.totalTasks * humanCostPerTask;
  
  // API 成本轉換（USD -> NTD，假設 1:32）
  const apiCostNTD = stats.totalCost * 32;
  
  return humanCost - apiCostNTD;
}

function calculateFTE(stats: DashboardStats): number {
  // 假設全職員工月處理 400 個任務
  return stats.totalTasks / 400;
}
```

---

## 5. 像素動畫規格

### 5.1 角色 Sprite

```
尺寸：32x32 像素
格式：PNG（透明背景）
動畫幀數：
- 閒置：4 幀（坐著呼吸）
- 工作：6 幀（打字/看螢幕）
- 開心：4 幀（舉手/跳躍）
- 疲倦：4 幀（趴桌/打哈欠）
- 走路：8 幀（用於切換位置）
```

### 5.2 辦公室場景

```
尺寸：320x180（16:9 比例）
元素：
- 背景：辦公室內部（窗戶、植物、海報）
- 工位：6 個座位，每個角色固定位置
- 裝飾：咖啡機、白板、檔案櫃
- 特效：螢幕光、打字特效、對話泡泡
```

### 5.3 動畫製作工具
- **Aseprite**：像素動畫製作
- **Piskel**：免費線上替代
- **AI 生成**：可用 Midjourney/DALL-E 生成參考再手繪

---

## 6. 技術選型

### 6.1 前端
- **框架**：React 18 + TypeScript
- **動畫**：Framer Motion + CSS Sprite Animation
- **圖表**：Recharts 或 Chart.js
- **UI 庫**：Tailwind CSS

### 6.2 後端（輕量）
- **資料收集**：定時讀取 OpenClaw sessions/cron
- **儲存**：SQLite（本地）或 JSON 檔案
- **API**：可用 OpenClaw Canvas 或獨立 Express

### 6.3 部署選項
- **Canvas**：整合進 OpenClaw，用 `canvas present`
- **本地 Web**：獨立 React app，`npm run dev`
- **公開部署**：Vercel / Netlify

---

## 7. MVP 範圍

### 第一版只做：
1. ✅ 靜態儀表板（手動更新數據）
2. ✅ 成本節省 + 互動統計兩個 tab
3. ✅ 員工排行榜
4. ⏳ 員工心聲（固定文案）
5. ⏳ 角色卡彈窗

### 第二版加入：
- 即時狀態更新
- 像素動畫場景
- 動態心聲生成
- 歷史趨勢圖表

---

## 8. 開發排程

| 週次 | 任務 | 產出 |
|------|------|------|
| Week 1 | 角色設計 + UI 稿 | 6 個角色設定、Figma 設計稿 |
| Week 2 | 前端基礎 | React 專案、儀表板骨架 |
| Week 3 | 資料整合 | OpenClaw API 串接、統計邏輯 |
| Week 4 | 動畫 + 上線 | 像素動畫、Canvas 整合 |

---

*設計文件 v1.0 — 2026/02/06*
